[project]
name = "openhost-elevate-backend"
version = "0.1.0"
description = "Serves the Elevate web build and proxies the OpenHost health-data service"
requires-python = ">=3.12"
dependencies = [
    "litestar[standard]>=2.12",
    "httpx>=0.27",
]

[tool.uv]
package = false
